[package]
name = "fast_confirmation_core"
version = "0.1.0"
edition = { workspace = true }
description = "The arithmetic and vote predicate of the fast confirmation rule, with no store and no std."

[dependencies]
